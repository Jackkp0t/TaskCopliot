# 实习生编程挑战：智能任务管理系统

## 概述

构建一个具备智能功能的**任务管理系统**，以展示你在不同专业方向上的编程能力。本挑战旨在评估后端、前端、全栈以及 AI/LLM 开发等岗位的候选人。

**时间限制：** 2–4 小时  
**提交方式：** Git 仓库，需包含清晰的 README 和提交历史

---

## 问题描述

创建一个任务管理系统，让用户能够在智能辅助下创建、整理和管理任务。系统应提供后端 API，也可以选择性地包含前端界面和/或 AI 驱动的功能。

---

## 核心要求（所有候选人）

### 1. 任务 CRUD 操作

实现 RESTful API 或服务层，并提供以下操作：

- **CREATE（创建）**——添加新任务
- **READ（读取）**——根据 ID 获取任务或列出全部任务
- **UPDATE（更新）**——修改任务详情
- **DELETE（删除）**——删除任务

### 2. 任务属性

每个任务应包含：

- `id`（唯一标识符）
- `title`（字符串，必填）
- `description`（字符串，可选）
- `status`（枚举值：`pending`、`in_progress`、`completed`）
- `priority`（枚举值：`low`、`medium`、`high`）
- `created_at`（时间戳）
- `updated_at`（时间戳）
- `tags`（字符串数组，可选）

### 3. 数据持久化

选择合适的存储方案：

- **简单方案：** JSON 文件或 SQLite
- **推荐方案：** MySQL、PostgreSQL、MongoDB 或 Redis
- **进阶方案：** 数据库与缓存层组合

### 4. 代码质量要求

- 代码整洁、易读且组织良好
- 合理的错误处理
- 输入校验
- 至少提供基础文档（README）
- 清晰的提交信息

---

## 岗位方向专项功能（根据目标岗位选择）

### 选项 A：后端开发方向

实现以下高级后端功能：

1. **任务筛选与排序**
   - 按状态、优先级和标签筛选
   - 按创建日期、优先级和状态排序
   - 支持分页

2. **任务依赖关系**
   - 任务可以依赖其他任务
   - 如果依赖任务尚未完成，则不能将当前任务标记为已完成
   - 提供查询任务依赖树的 API

3. **性能优化**
   - 对高频访问的数据实现缓存
   - 优化数据库查询
   - 基础操作的 API 响应时间低于 100 毫秒

4. **系统设计**
   - 考虑系统扩展至 10 万个以上任务时的设计方案
   - 提供一份简要的架构说明文档

**加分项：**

- 实现搜索功能（全文搜索）
- 添加用户身份认证与权限控制
- 对 API 端点进行限流
- 容器化（Docker）

---

### 选项 B：前端开发方向

构建包含以下功能的用户界面：

1. **任务仪表盘**
   - 以有序视图展示全部任务（列表/看板/日历）
   - 使用可视化标识展示优先级和状态
   - 响应式设计（适配移动端）

2. **交互式任务管理**
   - 通过拖放更改状态或调整顺序
   - 快捷操作（标记完成、删除、编辑）
   - 实时搜索与筛选

3. **用户体验**
   - 流畅的动画与过渡效果
   - 加载状态与错误提示
   - 表单校验，并提供有帮助的反馈

4. **状态管理**
   - 合理使用状态管理方案（Context/Redux/Vuex/Pinia）
   - 乐观式 UI 更新
   - 妥善处理边界情况

**可选技术栈：**

- React + TypeScript + Tailwind/MUI
- Vue 3 + TypeScript + Element Plus
- 原生 JavaScript（如果你希望展示基础能力）

**加分项：**

- 支持深色模式
- 键盘快捷键
- 离线支持（PWA）
- 数据可视化（图表）

---

### 选项 C：全栈开发方向

结合后端与前端要求：

1. **完整应用**
   - 可正常运行的后端 API（从后端方向功能中选择）
   - 功能完整的前端 UI（从前端方向功能中选择）
   - 前后端正确集成

2. **系统架构**
   - 清晰的关注点分离
   - 采用 RESTful API 或 GraphQL 设计
   - 前后端均具备错误处理机制

3. **部署考虑**
   - 提供 Docker Compose 配置或部署说明
   - 管理环境配置
   - 基础 CI/CD 配置（可选，但会给人留下良好印象）

**加分项：**

- 使用 WebSocket 实现实时更新
- 支持上传任务附件
- 导出任务（JSON/CSV/PDF）

---

### 选项 D：AI/LLM 开发方向

使用 AI/ML/LLM 构建智能功能：

1. **智能任务生成**
   - 根据自然语言创建任务，例如："提醒我明天下午 3 点购买日用品"
   - 从自然语言中提取标题、描述、截止日期和优先级

2. **智能任务整理**
   - 根据任务内容自动推荐标签
   - 使用简单的机器学习模型或 LLM 推荐优先级
   - 对任务进行分类

3. **AI 助手功能**（至少选择 2 项）
   - 任务拆解：将复杂任务拆分为子任务
   - 任务摘要：生成每日/每周任务摘要
   - 相似任务检测：查找相关任务
   - 智能搜索：使用语义搜索替代关键词匹配

4. **实现方案**
   - 使用 OpenAI API / Anthropic API（需妥善处理 API 密钥）
   - 使用本地模型（Hugging Face、LangChain）
   - 使用嵌入模型与向量数据库（ChromaDB、FAISS）
   - 构建简单的机器学习分类器（scikit-learn、PyTorch）

**加分项：**

- 为任务知识库实现 RAG
- 多智能体系统（规划智能体 + 执行智能体）
- 针对任务领域进行模型微调
- 提示词优化与测试

---

## 技术约束

### 后端框架（任选一种）

- **Python：** FastAPI、Flask、Django
- **Node.js：** Express、NestJS、Fastify
- **Java：** Spring Boot
- **Go：** Gin、Echo
- **其他：** 请说明选择理由

### 数据库选项

- SQL：PostgreSQL、MySQL、SQLite
- NoSQL：MongoDB、Redis
- 向量数据库（用于 AI 方向）：ChromaDB、Pinecone、FAISS

### 前端框架（如适用）

- React、Vue、Angular、Svelte
- 或原生 HTML/CSS/JavaScript

---

## 评估标准

### 代码质量（30%）

- 代码整洁、易读，风格统一
- 项目结构与组织合理
- 变量名和函数名语义清晰
- 在必要之处添加注释（不过度注释）
- 错误处理与边界情况处理

### 问题解决能力（30%）

- 正确实现需求
- 算法效率
- 边界情况处理
- 针对挑战提出有创意的解决方案

### 系统设计（25%）

- 合理的架构选择
- 可扩展性考虑
- 数据库模式设计
- API 设计（遵循 RESTful 原则或使用 GraphQL）
- 关注点分离

### 文档（15%）

- 提供清晰的 README，内容包括：
  - 项目说明
  - 配置与安装说明
  - API 文档
  - 设计决策与权衡
  - 已知限制
  - 未来改进方向
- 在适当位置添加代码注释
- 良好的提交历史

---

## 提交要求

### 1. Git 仓库

- 从项目开始时就初始化 Git
- 进行有意义的分次提交（不要只进行一次包含全部内容的大提交）
- 包含 `.gitignore` 文件
- **请勿提交：** API 密钥、`node_modules`、`venv`、IDE 配置文件

### 2. README.md

必须包含：

```markdown
# 项目名称

## 岗位方向
[后端 / 前端 / 全栈 / AI-LLM]

## 技术栈
- 编程语言：
- 框架：
- 数据库：
- 其他工具：

## 已实现功能
- [ ] 功能 1
- [ ] 功能 2
...

## 配置与运行说明
1. 前置条件
2. 安装步骤
3. 配置
4. 运行应用

## API 文档
[端点、请求/响应示例]

## 设计决策
[说明选择特定技术或方案的原因]

## 挑战与解决方案
[遇到的问题及解决方式]

## 未来改进
[如果有更多时间，你会添加什么]

## 所用时间
约 X 小时
```

### 3. 代码结构示例

```
project/
├── README.md
├── requirements.txt / package.json
├── .gitignore
├── src/
│   ├── models/
│   ├── controllers/
│   ├── services/
│   ├── routes/
│   └── utils/
├── tests/（可选，但推荐）
└── docs/（可选）
```

---

## 加分项（可选）

- **测试：** 单元测试、集成测试
- **CI/CD：** GitHub Actions 工作流
- **日志：** 实现结构化日志
- **监控：** 健康检查端点
- **安全性：** 防止 SQL 注入、XSS 防护、CORS 配置
- **性能：** 负载测试结果、优化说明文档
- **无障碍访问：** 符合 WCAG 标准（针对前端）

---

## 常见问题

**问：可以使用外部库/框架吗？**  
答：可以，但需要说明选择理由。使用过多不必要的库可能会影响评分。

**问：可以使用 AI 助手（ChatGPT、Copilot）吗？**  
答：可以，但请在 README 中注明。相比完美的代码，我们更看重你的理解能力和决策过程。

**问：如果无法完成所有内容怎么办？**  
答：提交当前已完成的内容，并在文档中清楚说明哪些内容已完成、哪些内容仍待完成。质量优于数量。

**问：可以选择多个方向吗？**  
答：能力较强的候选人可以组合多个方向（例如全栈 + AI 功能），但要确保质量不受影响。

**问：需要部署应用吗？**  
答：不作强制要求，但提供可在线访问的演示会给人留下良好印象。至少应提供清晰的本地配置与运行说明。

---

祝你好运！我们期待看到你的解决方案。请记住：相比功能是否全部完成，我们更看重**整洁的代码、良好的设计和清晰的思考过程**。
